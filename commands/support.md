---
description: Route an explicit auxiliary support request outside the main Requirement Flow delivery path.
argument-hint: "[support request]"
---

Use the `requirement-flow-plugin:support-router` skill for this request.

Input:

$ARGUMENTS

If the user names a specific support capability, route to the matching auxiliary support skill.
