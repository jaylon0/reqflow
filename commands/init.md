---
description: Scan the current project and prepare the minimum Requirement Flow context.
argument-hint: "[project context request]"
---

Use the `reqflow:context-bootstrap` skill for this request.

Input:

$ARGUMENTS

Scan before asking. Only create or update context files when implementation or setup is authorized.
