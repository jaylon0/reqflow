---
description: Run the Requirement Flow loop engine for a failed check, review finding, blocker, or repair cycle.
argument-hint: "[failure, finding, or blocker]"
---

Use the `requirement-flow-plugin:loop-engine` skill for this request.

Input:

$ARGUMENTS

If the request belongs to a main-flow run, read that run state before deciding the loop boundary.
