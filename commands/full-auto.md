---
description: Run the full PRD-to-code flow with all standard actions (multi-agent, consensus, quality gates, confidence, reflection) but auto-confirm at each stage. Only stops at final acceptance.
argument-hint: "[requirement or PRD]"
---

Use the `reqflow:full-auto` skill for this request.

Input:

$ARGUMENTS

If a resumable run exists, show the run status and ask whether to continue before starting a new run.
