# 用户标签管理功能

## 背景

系统需要支持给用户打标签，便于后续按标签筛选和分组。

## 需求

在现有项目中新增用户标签（UserTag）模块：

1. **数据模型**：新建 `user_tags` 表，字段：`id`, `user_id`, `tag_name`, `created_at`
2. **API 接口**：
   - `POST /api/tags` — 给用户添加标签（参数：user_id, tag_name）
   - `GET /api/tags?user_id=xxx` — 查询某用户的所有标签
   - `DELETE /api/tags/:id` — 删除指定标签
3. **校验规则**：
   - tag_name 非空，长度 1-50 字符
   - 同一用户不能有重复标签
4. **错误处理**：返回标准 JSON 错误格式 `{ "error": "message" }`

## 安全要求

- 所有输入参数必须做 SQL 注入防护
- tag_name 需要过滤特殊字符（XSS 防护）
- API 需要增加接口鉴权校验（检查 token）

## 部署要求

- 提供数据库迁移脚本（DDL）
- 配置文件中增加数据库连接参数占位

## 技术约束

- 复用现有项目的框架和 ORM
- 遵循现有项目的目录结构和命名规范
- 需要写单元测试

## 验收标准

- [ ] 三个 API 接口可用
- [ ] 校验规则生效
- [ ] 安全防护生效（SQL注入、XSS、鉴权）
- [ ] 数据库迁移脚本可用
- [ ] 单元测试通过
